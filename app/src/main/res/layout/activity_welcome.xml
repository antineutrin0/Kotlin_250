<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="24dp"
    android:background="@color/tab_inactive_text"
    android:gravity="center">

    <!-- App Icon with Badge -->
    <RelativeLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="32dp">

        <ImageView
            android:id="@+id/app_icon"
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:src="@drawable/ic_unary_logo"
            android:background="@drawable/rounded_purple_bg"
            android:padding="24dp"
            android:scaleType="centerInside" />

        <ImageView
            android:id="@+id/badge"
            android:layout_width="42dp"
            android:layout_height="42dp"
            android:layout_alignTop="@id/app_icon"
            android:layout_alignEnd="@id/app_icon"
            android:layout_marginTop="0dp"
            android:layout_marginEnd="0dp"
            android:padding="4dp"
            android:src="@drawable/ic_book"
            android:background="@drawable/circular_orange_bg"
            />

    </RelativeLayout>

    <!-- Welcome Text -->
    <TextView
        android:id="@+id/welcome_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Welcome to"
        android:textSize="24sp"
        android:textColor="@color/text_secondary"
        android:layout_marginBottom="12dp" />

    <!-- App Name -->
    <TextView
        android:id="@+id/app_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Unary"
        android:textSize="48sp"
        android:textColor="@color/purple_primary"
        android:textStyle="bold"
        android:layout_marginBottom="16dp" />

    <!-- Subtitle -->
    <TextView
        android:id="@+id/subtitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Admission Preparation Platform"
        android:textSize="18sp"
        android:textColor="@color/orange_accent"
        android:textStyle="bold"
        android:layout_marginBottom="24dp" />

    <!-- Description -->
    <TextView
        android:id="@+id/description"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Your personal non-boring universe with cosmic\nchoice of actual courses"
        android:textSize="16sp"
        android:textColor="@color/text_secondary"
        android:textAlignment="center"
        android:lineSpacingExtra="4dp"
        android:layout_marginBottom="48dp" />

    <!-- Features Section -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginBottom="48dp"
        android:gravity="center">

        <!-- Interactive Courses -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical"
            android:gravity="center"
            android:padding="16dp">

            <ImageView
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:src="@drawable/ic_book_open"
                android:background="@drawable/circular_purple_light_bg"
                android:padding="12dp"
                android:layout_marginBottom="12dp" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Interactive\nCourses"
                android:textSize="14sp"
                android:textColor="@color/text_primary"
                android:textAlignment="center"
                android:textStyle="bold" />

        </LinearLayout>

        <!-- Expert Teachers -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical"
            android:gravity="center"
            android:padding="16dp">

            <ImageView
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:src="@drawable/ic_teacher"
                android:background="@drawable/circular_orange_light_bg"
                android:padding="12dp"
                android:layout_marginBottom="12dp" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Expert Teachers"
                android:textSize="14sp"
                android:textColor="@color/text_primary"
                android:textAlignment="center"
                android:textStyle="bold" />

        </LinearLayout>

        <!-- Proven Results -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical"
            android:gravity="center"
            android:padding="16dp">

            <ImageView
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:src="@drawable/ic_trophy"
                android:background="@drawable/circular_green_light_bg"
                android:padding="12dp"
                android:layout_marginBottom="12dp" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Proven Results"
                android:textSize="14sp"
                android:textColor="@color/text_primary"
                android:textAlignment="center"
                android:textStyle="bold" />

        </LinearLayout>

    </LinearLayout>

    <!-- Sign Up Button -->
    <Button
        android:id="@+id/btnSignUp"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:text="Sign Up"
        android:textColor="@android:color/white"
        android:textSize="18sp"
        android:textStyle="bold"
        android:background="@drawable/purple_gradient_button"
        android:layout_marginBottom="16dp"
        android:drawableEnd="@drawable/ic_arrow_forward"
        android:drawablePadding="8dp"
        android:gravity="center" />

    <!-- Log In Button -->
    <Button
        android:id="@+id/btnLogIn"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:text="Log In"
        android:textColor="@android:color/white"
        android:textSize="18sp"
        android:textStyle="bold"
        android:startColor="#FF9800"
        android:endColor="#E55722"
        android:gravity="center" />

</LinearLayout>